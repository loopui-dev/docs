diff --git a/user-components/Icon.astro b/user-components/Icon.astro
index 16b24dee5916ea734356efa0ea0e3ee204ca5083..0680cf726582a3e9f41aed42dd87a2587b6a3f77 100644
--- a/user-components/Icon.astro
+++ b/user-components/Icon.astro
@@ -1,42 +1,89 @@
 ---
 import { Icons, type StarlightIcon } from '../components/Icons';
+import fs from 'node:fs';
+import path from 'node:path';
 
 interface Props {
-	name: StarlightIcon;
-	label?: string;
-	color?: string;
-	size?: string;
-	class?: string;
+  name: StarlightIcon | string;
+  label?: string;
+  color?: string;
+  size?: string;
+  class?: string;
 }
 
 const { name, label, size = '1em', color } = Astro.props;
-const a11yAttrs = label ? ({ 'aria-label': label } as const) : ({ 'aria-hidden': 'true' } as const);
+const a11yAttrs = label
+  ? ({ 'aria-label': label } as const)
+  : ({ 'aria-hidden': 'true' } as const);
 
-/**
- * The fragment around the element is used as a workaround to avoid a trailing whitespace in the output.
- * @see https://github.com/withastro/compiler/issues/1003
- */
+let innerSvg: string;
+let viewBox = '0 0 24 24'; // default for built-in icons
+
+if (typeof name === 'string' && name.endsWith('.svg')) {
+  // Custom SVG from /public
+  const rel = name.startsWith('/') ? name.slice(1) : name;
+  const filePath = path.join(process.cwd(), 'public', rel);
+  const raw = fs.readFileSync(filePath, 'utf8');
+
+  // Extract viewBox + inner content from the original <svg>
+  const match = raw.match(
+    /<svg[^>]*viewBox="([^"]+)"[^>]*>([\s\S]*?)<\/svg>/i,
+  );
+
+  if (match) {
+    viewBox = match[1];
+    innerSvg = match[2];
+  } else {
+    // Fallback: no wrapping <svg>, just use the raw content
+    innerSvg = raw;
+  }
+
+  // Normalize fill/stroke attributes
+  innerSvg = innerSvg
+    .replace(/fill="(?!none)[^"]*"/gi, 'fill="currentColor"')
+    .replace(/stroke="(?!none)[^"]*"/gi, 'stroke="currentColor"');
+
+  // Normalize inline style fills/strokes (e.g. style="fill:white;stroke:#000;")
+  innerSvg = innerSvg.replace(
+    /style="([^"]*)"/gi,
+    (_m, style: string) => {
+      let s = style
+        .replace(
+          /fill\s*:\s*(?!none)[^;"]*/gi,
+          'fill:currentColor',
+        )
+        .replace(
+          /stroke\s*:\s*(?!none)[^;"]*/gi,
+          'stroke:currentColor',
+        );
+      return `style="${s}"`;
+    },
+  );
+} else {
+  // Built-in Starlight icon
+  innerSvg = Icons[name as StarlightIcon];
+}
 ---
 
 <>
-	<svg
-		{...a11yAttrs}
-		class={Astro.props.class}
-		width="16"
-		height="16"
-		viewBox="0 0 24 24"
-		fill="currentColor"
-		set:html={Icons[name]}
-	/>
+  <svg
+    {...a11yAttrs}
+    class={Astro.props.class}
+    width="16"
+    height="16"
+    viewBox={viewBox}
+    fill="currentColor"
+    set:html={innerSvg}
+  />
 </>
 
 <style define:vars={{ 'sl-icon-color': color, 'sl-icon-size': size }}>
-	@layer starlight.components {
-		svg {
-			color: var(--sl-icon-color);
-			font-size: var(--sl-icon-size, 1em);
-			width: 1em;
-			height: 1em;
-		}
-	}
+  @layer starlight.components {
+    svg {
+      color: var(--sl-icon-color);
+      font-size: var(--sl-icon-size, 1em);
+      width: 1em;
+      height: 1em;
+    }
+  }
 </style>
diff --git a/user-components/TabItem.astro b/user-components/TabItem.astro
index 0bd9fa6df544d380f3636f7e85ee829d41581ed0..7676fde22c44abf8fba8ef12c52be5d5aafe569a 100644
--- a/user-components/TabItem.astro
+++ b/user-components/TabItem.astro
@@ -3,17 +3,26 @@ import { TabItemTagname } from './rehype-tabs';
 import type { StarlightIcon } from '../components/Icons';
 
 interface Props {
-	icon?: StarlightIcon;
+	icon?: StarlightIcon | string | {light?: StarlightIcon | string, dark?: StarlightIcon | string};
 	label: string;
+	size?: string;
 }
 
-const { icon, label } = Astro.props;
+const { icon, label, size } = Astro.props;
 
 if (!label) {
 	throw new Error('Missing prop `label` on `<TabItem>` component.');
 }
+
+let lightIcon = icon;
+let darkIcon: string | undefined;
+
+if (typeof icon !== 'string') {
+	lightIcon = icon?.light;
+	darkIcon = icon?.dark;
+}
 ---
 
-<TabItemTagname data-label={label} data-icon={icon}>
+<TabItemTagname data-label={label} data-icon={lightIcon} data-icon-dark={darkIcon} data-size={size}>
 	<slot />
 </TabItemTagname>
diff --git a/user-components/Tabs.astro b/user-components/Tabs.astro
index d8fa647889eac3c41717e1ab8f6f533ad03d0ed9..827d695f67a8b987e0b7d9d30fa113505fed0ca9 100644
--- a/user-components/Tabs.astro
+++ b/user-components/Tabs.astro
@@ -30,11 +30,9 @@ const { html, panels } = processPanels(panelHtml);
  */
 const isSynced = syncKey !== undefined;
 const didRenderSyncedTabsRestoreScriptSymbol = Symbol.for('starlight:did-render-synced-tabs-restore-script');
-// @ts-expect-error - See above
 const shouldRenderSyncedTabsRestoreScript = isSynced && Astro.locals[didRenderSyncedTabsRestoreScriptSymbol] !== true;
 
 if (isSynced) {
-	// @ts-expect-error - See above
 	Astro.locals[didRenderSyncedTabsRestoreScriptSymbol] = true
 }
 ---
@@ -75,7 +73,8 @@ if (isSynced) {
 		panels && (
 			<div class="tablist-wrapper not-content">
 				<ul role="tablist">
-					{panels.map(({ icon, label, panelId, tabId }, idx) => (
+					{panels.map(({ icon, iconDark, label, size, panelId, tabId }, idx) => {
+						return(
 						<li role="presentation" class="tab">
 							<a
 								role="tab"
@@ -84,11 +83,26 @@ if (isSynced) {
 								aria-selected={idx === 0 ? 'true' : 'false'}
 								tabindex={idx !== 0 ? -1 : 0}
 							>
-								{icon && <Icon name={icon} />}
+								{icon && (
+								<>
+									{/* Light mode icon */}
+									<Icon
+									name={icon}
+									size={size}
+									class="sl-tab-icon sl-tab-icon--light"
+									/>
+									{/* Dark mode icon (fallback to light if not provided) */}
+									<Icon
+									name={iconDark ?? icon}
+									size={size}
+									class="sl-tab-icon sl-tab-icon--dark"
+									/>
+								</>
+								)}
 								{label}
 							</a>
 						</li>
-					))}
+					)})}
 				</ul>
 			</div>
 		)
@@ -99,6 +113,18 @@ if (isSynced) {
 
 <style>
 	@layer starlight.components {
+		.sl-tab-icon--dark {
+			display: none;
+		}
+
+		html[data-theme='dark'] .sl-tab-icon--dark {
+			display: inline-block;
+		}
+
+		html[data-theme='dark'] .sl-tab-icon--light {
+			display: none;
+		}
+
 		starlight-tabs {
 			display: block;
 		}
diff --git a/user-components/rehype-tabs.ts b/user-components/rehype-tabs.ts
index b989a81fda897aedfbbd9438d62eeca1a4cc176d..53f1837ff169ff6f0804898e6477781171ee5bec 100644
--- a/user-components/rehype-tabs.ts
+++ b/user-components/rehype-tabs.ts
@@ -8,7 +8,9 @@ interface Panel {
 	panelId: string;
 	tabId: string;
 	label: string;
+	size?: string;
 	icon?: StarlightIcon;
+	iconDark?: StarlightIcon;
 }
 
 declare module 'vfile' {
@@ -61,13 +63,15 @@ const tabsProcessor = rehype()
 					return CONTINUE;
 				}
 
-				const { dataLabel, dataIcon } = node.properties;
+				const { dataLabel, dataIcon, dataIconDark, dataSize } = node.properties;
 				const ids = getIDs();
 				const panel: Panel = {
 					...ids,
 					label: String(dataLabel),
 				};
 				if (dataIcon) panel.icon = String(dataIcon) as StarlightIcon;
+				if (dataIconDark) panel.iconDark = String(dataIconDark) as StarlightIcon;
+				if (dataSize) panel.size = String(dataSize);
 				file.data.panels?.push(panel);
 
 				// Remove `<TabItem>` props
